# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MusicAPI' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MusicAPI
  pod "SwiftyCam"
  pod 'SwiftyJSON'
  pod 'Alamofire'
  pod 'SwiftVideoGenerator'
  pod 'SDWebImage'
  pod 'NVActivityIndicatorView'

  target 'MusicAPITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MusicAPIUITests' do
    # Pods for testing
  end

end
